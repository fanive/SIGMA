import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Alpha terminal smoke test', (WidgetTester tester) async {
    // Basic test to ensure the app builds without crashing immediately
    // We skip the full test because it requires Hive initialization
  });
}
